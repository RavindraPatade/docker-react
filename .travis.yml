sudo: required
services :
    - docker

before_install:
    - docker build -t ravindrapatade/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true ravindrapatade/docker-react npm run test
